PROTEIN:
  MODE: "sequence"  # Use sequence mode instead of graph mode
  EMBEDDING_DIM: 128  # Adjusted for sequence mode
  SEQUENCE:
    CHAR_DIM: 128
    NUM_FILTERS: [128, 128, 128]
    KERNEL_SIZE: [3, 6, 9]

DRUG:
  ATOM_HIDDEN_DIM: [128, 64]  # Adjusted to match protein embedding

SOLVER:
  MAX_EPOCH: 70  # Reduced for testing
  BATCH_SIZE: 16   # Reduced for testing

DECODER:
  IN_DIM: 128     # Adjusted to match protein embedding

RESULT:
  OUTPUT_DIR: "./result_sequence_mode"
  SAVE_MODEL: False 